
.env:12:DJANGO_ALLOWED_HOSTS=localhost,127.0.0.1
.env:26:# --- CORS (space-separated origins, e.g. the deployed dashboard origin) ---
.env:28:# (create user, assignments, etc.) are not silently blocked by CORS.
.env:29:CORS_ALLOWED_ORIGINS=http://localhost:3000,http://localhost:3999,http://localhost:8000


